//Write different programs to find out if the given numvber(entered by the user) is armstrong number or not using for loop 

#include<stdio.h>
int main()
{
  int num; printf("Enter a number:");
  scanf("%d", &num);
  for (int i=1;)
}

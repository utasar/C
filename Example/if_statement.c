#include<stdio.h>
int main()
{
    //declare variable
    int age;
// initialize variable
age=19;
//check the condition
int result=(age>=18);
printf("The condition for if is %d\n ",result);
   //check if the person can vote
   if (age>=18)
   {
    //display if the condition is satisfied
    printf("You are elegible to vote!\n");
   }
  // printf("code runs here\n");
   return 0;
}
